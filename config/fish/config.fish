set -U Z_NO_RESOLVE_SYMLINKS 1
set -U Z_CMD j
set -U EDITOR vim
set -U PAGER less

alias ls "ls -F --color=auto"
alias ll "ls -l"
alias la "ls -a"
alias lla "ls -la"
alias g "git"
alias gi "git"
alias gu "git pull --rebase"

# function cdup
#   cd ..
# end
