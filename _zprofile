if [ $SHLVL = 2 -a -d ${HOME}/.rbenv  ] ; then
  # export PATH="${HOME}/.rbenv/bin:${HOME}/.rbenv/shims:${PATH}"
  # PATH=${HOME}/.rbenv/shims:${PATH}
  eval "$(rbenv init - zsh)"
fi

if [ $SHLVL = 2 -a -d ${HOME}/.pyenv  ] ; then
  if which pyenv > /dev/null; then eval "$(pyenv init -)"; fi
fi
